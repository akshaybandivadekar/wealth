import React from 'react';

const TransactionTable = ({ transactions }) => {
  return (
    <div>
      {/* Filters */}
      {/* Transaction Table */}
    </div>
  );
};

export default TransactionTable;
